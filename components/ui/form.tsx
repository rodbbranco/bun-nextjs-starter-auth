// Re-exported for backward compatibility.
// Use `@/components/ui/field` directly for new code.
export {
  Field as FormItem,
  FieldLabel as FormLabel,
  FieldContent as FormControl,
  FieldError as FormMessage,
  FieldDescription,
} from "@/components/ui/field"
